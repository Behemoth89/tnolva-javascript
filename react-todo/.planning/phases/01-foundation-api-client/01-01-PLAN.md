---
phase: 01-foundation-api-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/index.css
  - src/App.tsx
  - src/main.tsx
  - index.html
  - .env.example
  - .env
  - .gitignore
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - src/features/auth/pages/LoginPage.tsx
  - src/features/auth/pages/RegisterPage.tsx
  - src/features/dashboard/pages/DashboardPage.tsx
autonomous: true
requirements: [INF-01, INF-02]
must_haves:
  truths:
    - "Vite dev server starts and serves the app"
    - "Tailwind CSS classes render styled elements"
    - "React Router navigates between /login, /register, /dashboard"
    - "Placeholder pages display without errors"
  artifacts:
    - path: "package.json"
      provides: "Dependencies: react, react-dom, react-router, @heroicons/react, zustand, axios, tailwindcss, @tailwindcss/vite, @vitejs/plugin-react"
      contains: "react"
    - path: "vite.config.ts"
      provides: "Vite config with React and Tailwind plugins"
      contains: "@tailwindcss/vite"
    - path: ".gitignore"
      provides: "Git ignore rules for node_modules, dist, .env, IDE files"
      contains: "node_modules"
    - path: "src/index.css"
      provides: "Tailwind CSS import"
      contains: "@import \"tailwindcss\""
    - path: "src/App.tsx"
      provides: "Router setup with placeholder routes"
      exports: ["App"]
    - path: "src/features/auth/pages/LoginPage.tsx"
      provides: "Login placeholder page"
      contains: "Login"
    - path: "src/features/auth/pages/RegisterPage.tsx"
      provides: "Register placeholder page"
      contains: "Register"
    - path: "src/features/dashboard/pages/DashboardPage.tsx"
      provides: "Dashboard placeholder page"
      contains: "Dashboard"
  key_links:
    - from: "src/App.tsx"
      to: "src/features/*/pages/*.tsx"
      via: "React Router <Route> elements"
      pattern: "Route.*element=.*LoginPage|RegisterPage|DashboardPage"
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "Tailwind CSS plugin processes CSS"
      pattern: "@import.*tailwindcss"
---

<objective>
Scaffold the React project with Vite, Tailwind CSS, and React Router SPA mode. Create placeholder pages for /login, /register, and /dashboard routes.

Purpose: Establish the foundational build tooling, styling system, and routing structure that all subsequent phases depend on.
Output: Running Vite dev server with Tailwind CSS and three placeholder pages.
</objective>

<execution_context>
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-api-client/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + Tailwind CSS project</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, src/index.css, src/main.tsx, index.html, .env.example, .env</files>
  <action>
    Initialize the project with Vite 8, React 19.2, and Tailwind CSS 4.2 per decisions D-01, D-08, D-12, and requirement INF-01.

    1. Create package.json with dependencies:
       - react@^19.2.0, react-dom@^19.2.0
       - react-router@^7.14.0 (per D-12)
       - zustand@^5.0.0 (per D-03)
       - axios@^1.7.0 (per D-06)
       - @heroicons/react@^2.2.0
       - Dev: typescript@^5.7.0, vite@^8.0.0, @vitejs/plugin-react@^6.0.0, @tailwindcss/vite@^4.2.0, tailwindcss@^4.2.0, @types/react@^19.0.0, @types/react-dom@^19.0.0

    2. Create vite.config.ts:
       - Import defineConfig from 'vite'
       - Import react from '@vitejs/plugin-react'
       - Import tailwindcss from '@tailwindcss/vite'
       - Export defineConfig({ plugins: [react(), tailwindcss()] })

    3. Create tsconfig.json (root config referencing app and node configs):
       - { "files": [], "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }] }

    4. Create tsconfig.app.json:
       - { "compilerOptions": { "target": "ES2020", "useDefineForClassFields": true, "lib": ["ES2020", "DOM", "DOM.Iterable"], "module": "ESNext", "skipLibCheck": true, "moduleResolution": "bundler", "allowImportingTsExtensions": true, "isolatedModules": true, "moduleDetection": "force", "noEmit": true, "jsx": "react-jsx", "strict": true, "noUnusedLocals": true, "noUnusedParameters": true, "noFallthroughCasesInSwitch": true, "noUncheckedSideEffectImports": true }, "include": ["src"] }

    5. Create tsconfig.node.json:
       - { "compilerOptions": { "target": "ES2022", "lib": ["ES2023"], "module": "ESNext", "skipLibCheck": true, "moduleResolution": "bundler", "allowImportingTsExtensions": true, "isolatedModules": true, "moduleDetection": "force", "noEmit": true, "strict": true, "noUnusedLocals": true, "noUnusedParameters": true, "noFallthroughCasesInSwitch": true, "noUncheckedSideEffectImports": true }, "include": ["vite.config.ts", "react-router.config.ts"] }

    6. Create src/index.css with: @import "tailwindcss";

    7. Create src/main.tsx:
       - Import React, ReactDOM, App, and './index.css'
       - Create root with ReactDOM.createRoot(document.getElementById('root')!)
       - Render <App />

    8. Create index.html with standard Vite template, linking to /src/main.tsx

    9. Create .env.example with: VITE_API_BASE_URL=https://taltech.akaver.com/api/v1
    10. Create .env with: VITE_API_BASE_URL=https://taltech.akaver.com/api/v1

    11. Create .gitignore with:
        - node_modules/
        - dist/
        - .env
        - .env.local
        - *.tsbuildinfo
        - .vscode/ (except settings/extensions)
        - .idea/

    12. Run npm install to install all dependencies.
  </action>
  <verify>
    <automated>npx vite build --mode development 2>&1 | grep -c "built in" || npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Project builds without errors, Tailwind CSS configured, TypeScript strict mode enabled, .env files created with VITE_API_BASE_URL</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder pages and router setup</name>
  <files>src/App.tsx, src/features/auth/pages/LoginPage.tsx, src/features/auth/pages/RegisterPage.tsx, src/features/dashboard/pages/DashboardPage.tsx</files>
  <action>
    Create the directory structure and placeholder pages per decisions D-01, D-13, and requirement INF-02.

    1. Create src/features/auth/pages/LoginPage.tsx:
       - Export function LoginPage() returning a div with text "Login Page" and Tailwind classes for basic centering (min-h-screen, flex, items-center, justify-center)

    2. Create src/features/auth/pages/RegisterPage.tsx:
       - Export function RegisterPage() returning a div with text "Register Page" and same centering classes

    3. Create src/features/dashboard/pages/DashboardPage.tsx:
       - Export function DashboardPage() returning a div with text "Dashboard Page" and same centering classes

    4. Create src/App.tsx with React Router SPA mode setup:
       - Import BrowserRouter, Routes, Route from 'react-router'
       - Import the three placeholder pages
       - Export function App() with:
         <BrowserRouter>
           <Routes>
             <Route path="/login" element={<LoginPage />} />
             <Route path="/register" element={<RegisterPage />} />
             <Route path="/dashboard" element={<DashboardPage />} />
             <Route path="*" element={<Navigate to="/login" replace />} />
           </Routes>
         </BrowserRouter>
       - Import Navigate from 'react-router'

    Note: Do NOT add ProtectedRoute wrapper yet — that comes in Plan 03. This plan only establishes the route structure.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Three placeholder pages exist at correct paths, App.tsx has BrowserRouter with Routes for /login, /register, /dashboard, and catch-all redirect to /login</done>
</task>

</tasks>

<verification>
- npm install completes without errors
- npx tsc --noEmit passes with zero errors
- npx vite build completes successfully
- Vite dev server starts (npx vite --port 5173)
- Visiting /login, /register, /dashboard shows placeholder text
- Tailwind CSS classes render (centered text visible)
</verification>

<success_criteria>
- Project scaffolding complete with Vite 8, React 19.2, Tailwind CSS 4.2, TypeScript strict mode
- Feature-based directory structure created (src/features/auth/pages/, src/features/dashboard/pages/)
- React Router SPA mode configured (ssr: false)
- Three placeholder pages render at their respective routes
- .env.example and .env created with VITE_API_BASE_URL
- INF-01 (Tailwind CSS) satisfied
- INF-02 (React Router) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-client/01-01-SUMMARY.md`
</output>
