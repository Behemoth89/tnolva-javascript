---
phase: 01-foundation-api-client
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/useAuthStore.ts
  - src/types/auth.ts
autonomous: true
requirements: [AUTH-04, AUTH-05]
must_haves:
  truths:
    - "Auth store persists token and user info in localStorage"
    - "Auth store exposes setAuth and clearAuth actions"
    - "Token is accessible for API interceptor injection"
    - "Store shape matches: { token, refreshToken, firstName, lastName, isAuthenticated, setAuth, clearAuth }"
  artifacts:
    - path: "src/types/auth.ts"
      provides: "Auth type definitions"
      contains: "interface AuthState"
      exports: ["AuthState", "SetAuthPayload"]
    - path: "src/stores/useAuthStore.ts"
      provides: "Zustand auth store with persist middleware"
      contains: "persist"
      exports: ["useAuthStore"]
  key_links:
    - from: "src/stores/useAuthStore.ts"
      to: "src/types/auth.ts"
      via: "import of AuthState type"
      pattern: "import.*AuthState.*from.*types/auth"
    - from: "src/stores/useAuthStore.ts"
      to: "localStorage"
      via: "Zustand persist middleware"
      pattern: "persist.*name.*auth"
---

<objective>
Create Zustand auth store with persist middleware for token and user state management.

Purpose: Provide the centralized auth state that the API client interceptor reads from (AUTH-04) and that the refresh flow updates (AUTH-05). All authenticated features depend on this store.
Output: Auth store with TypeScript types, persist middleware, and setAuth/clearAuth actions.
</objective>

<execution_context>
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-api-client/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define auth type contracts</name>
  <files>src/types/auth.ts</files>
  <read_first>
    - src/types/ (read to see if types directory exists and any existing patterns)
  </read_first>
  <action>
    Create src/types/auth.ts with TypeScript interfaces per decision D-05 (auth store shape).

    1. Export interface AuthState:
       - token: string | null
       - refreshToken: string | null
       - firstName: string | null
       - lastName: string | null
       - isAuthenticated: boolean

    2. Export interface SetAuthPayload (what setAuth accepts):
       - token: string
       - refreshToken: string
       - firstName: string
       - lastName: string

    3. Export interface LoginResponse (shape of API login response):
       - token: string
       - refreshToken: string
       - firstName: string
       - lastName: string

    4. Export interface RegisterPayload (what register endpoint accepts):
       - email: string
       - password: string
       - firstName: string
       - lastName: string

    5. Export interface LoginPayload (what login endpoint accepts):
       - email: string
       - password: string

    These are contracts only — no implementation. The API client (Plan 03) will use LoginResponse and LoginPayload. The auth store will use AuthState and SetAuthPayload.
  </action>
  <acceptance_criteria>
    - src/types/auth.ts exists and exports AuthState, SetAuthPayload, LoginResponse, RegisterPayload, LoginPayload
    - AuthState contains fields: token, refreshToken, firstName, lastName, isAuthenticated
    - All fields typed correctly (string | null for state fields, string for payload fields)
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Type file exists with all five exported interfaces, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Zustand auth store with persist middleware</name>
  <files>src/stores/useAuthStore.ts</files>
  <read_first>
    - src/types/auth.ts (just created — contains AuthState and SetAuthPayload)
    - src/stores/ (read to see if stores directory exists)
  </read_first>
  <action>
    Create src/stores/useAuthStore.ts per decisions D-03, D-04, D-05.

    1. Import create from 'zustand'
    2. Import { persist } from 'zustand/middleware'
    3. Import { AuthState, SetAuthPayload } from '../types/auth'

    4. Define store interface extending AuthState:
       - All AuthState fields (token, refreshToken, firstName, lastName, isAuthenticated)
       - setAuth: (payload: SetAuthPayload) => void
       - clearAuth: () => void

    5. Create store with create<persist<AuthStore>>():
       - Wrap with persist middleware: persist(create(...), { name: 'auth-storage' })
       - Initial state: all fields null/false (token: null, refreshToken: null, firstName: null, lastName: null, isAuthenticated: false)
       - setAuth: sets all four fields from payload, sets isAuthenticated to true
       - clearAuth: resets all fields to null/false

    6. Export the store as useAuthStore

    Do NOT add any API logic here — this store is pure state. The API client interceptors (Plan 03) will read from this store.
  </action>
  <acceptance_criteria>
    - src/stores/useAuthStore.ts exists and exports useAuthStore
    - Store uses zustand's create() with persist middleware
    - persist config has name: 'auth-storage'
    - Initial state has token: null, refreshToken: null, firstName: null, lastName: null, isAuthenticated: false
    - setAuth action accepts SetAuthPayload and sets all fields plus isAuthenticated: true
    - clearAuth action resets all fields to initial state
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>Auth store created with persist middleware, correct initial state, setAuth and clearAuth actions working, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes with zero errors
- Auth store types match decision D-05 shape exactly
- persist middleware configured with name 'auth-storage'
- Store can be imported and used: const { token, setAuth } = useAuthStore()
- localStorage contains auth-storage key after setAuth called
- clearAuth resets all fields to null/false
</verification>

<success_criteria>
- AUTH-04 foundation ready: Auth store provides token that API client interceptor will read from
- AUTH-05 foundation ready: Auth store's setAuth action will be called by refresh interceptor with new tokens
- TypeScript types defined for all auth-related data shapes
- Zustand store with persist middleware stores auth state in localStorage
- No API client logic in this plan — pure state management only
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-client/01-02-SUMMARY.md`
</output>
