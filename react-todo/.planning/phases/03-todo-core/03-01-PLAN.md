---
phase: 03-todo-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/task.ts
  - src/types/category.ts
  - src/types/priority.ts
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - TASK-05
  - CAT-01
  - CAT-02
  - CAT-03
  - PRI-01
  - PRI-02
  - PRI-03
  - INF-04
  - INF-05

must_haves:
  truths:
    - "Type contracts exist for tasks, categories, and priorities"
    - "Types match TalTech API response shapes exactly"
    - "Types are exported and importable by stores and components"
  artifacts:
    - path: "src/types/task.ts"
      provides: "Task type definitions matching API schema"
      exports: ["Task", "CreateTaskPayload", "UpdateTaskPayload"]
    - path: "src/types/category.ts"
      provides: "Category type definitions"
      exports: ["Category", "CreateCategoryPayload", "UpdateCategoryPayload"]
    - path: "src/types/priority.ts"
      provides: "Priority type definitions"
      exports: ["Priority", "CreatePriorityPayload", "UpdatePriorityPayload"]
  key_links:
    - from: "src/types/task.ts"
      to: "TalTech API /Task endpoints"
      via: "Type shape matching"
      pattern: "taskName|taskSort|isCompleted|todoCategoryId|todoPriorityId"
---

<objective>
Create TypeScript type contracts for tasks, categories, and priorities that match the TalTech API schema. These types will be the foundation for Zustand stores, API calls, and UI components.

Purpose: Establish type safety across the entire todo core - stores, API calls, and components will all use these contracts.
Output: Three type definition files with complete interfaces for CRUD operations.
</objective>

<execution_context>
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-todo-core/03-CONTEXT.md
@src/types/auth.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/types/auth.ts (pattern to follow):
```typescript
export interface AuthState {
  token: string | null;
  refreshToken: string | null;
  firstName: string | null;
  lastName: string | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}

export interface SetAuthPayload {
  token: string;
  refreshToken: string;
  firstName: string;
  lastName: string;
}

export interface LoginResponse {
  token: string;
  refreshToken: string;
  firstName: string;
  lastName: string;
}
```

From .planning/PROJECT.md (API schema):
- Task fields: id, taskName, taskSort, createdDt, dueDt, isCompleted, isArchived, todoCategoryId, todoPriorityId, syncDt
- Category fields: id, categoryName, categorySort, syncDt, tag
- Priority fields: id, priorityName, prioritySort, syncDt, tag
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Task type contracts</name>
  <files>src/types/task.ts</files>
  <action>Create src/types/task.ts with TypeScript interfaces matching the TalTech API Task schema. Follow the pattern from src/types/auth.ts.

Required interfaces:
```typescript
export interface Task {
  id: string;
  taskName: string;
  taskSort: number;
  createdDt: string;
  dueDt: string | null;
  isCompleted: boolean;
  isArchived: boolean;
  todoCategoryId: string | null;
  todoPriorityId: string | null;
  syncDt: string | null;
}

export interface CreateTaskPayload {
  taskName: string;
  dueDt?: string | null;
  todoCategoryId?: string | null;
  todoPriorityId?: string | null;
}

export interface UpdateTaskPayload extends Partial<CreateTaskPayload> {
  id: string;
  isCompleted?: boolean;
  taskSort?: number;
}
```

Key decisions from CONTEXT.md:
- D-02: Task cards show name, category, priority icon, due date, completion checkbox
- D-05: Form fields include task name, category dropdown, priority dropdown, due date picker
- All date fields are strings (ISO format from API)
- Nullable fields use `| null` not `undefined` (consistent with auth types)
</action>
  <verify>
    <automated>npx tsc --noEmit src/types/task.ts</automated>
  </verify>
  <done>Task interface matches API schema exactly, CreateTaskPayload and UpdateTaskPayload exported, file compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Category and Priority type contracts</name>
  <files>src/types/category.ts, src/types/priority.ts</files>
  <action>Create two type files following the same pattern as src/types/auth.ts and the new src/types/task.ts.

src/types/category.ts:
```typescript
export interface Category {
  id: string;
  categoryName: string;
  categorySort: number;
  syncDt: string | null;
  tag: string | null;
}

export interface CreateCategoryPayload {
  categoryName: string;
  categorySort?: number;
  tag?: string | null;
}

export interface UpdateCategoryPayload extends Partial<CreateCategoryPayload> {
  id: string;
}
```

src/types/priority.ts:
```typescript
export interface Priority {
  id: string;
  priorityName: string;
  prioritySort: number;
  syncDt: string | null;
  tag: string | null;
}

export interface CreatePriorityPayload {
  priorityName: string;
  prioritySort?: number;
  tag?: string | null;
}

export interface UpdatePriorityPayload extends Partial<CreatePriorityPayload> {
  id: string;
}
```

Design notes:
- Category and Priority have identical structure (name, sort, syncDt, tag)
- Both use string IDs (UUIDs from API)
- sort fields default to 0 for new items
- tag field is nullable metadata
</action>
  <verify>
    <automated>npx tsc --noEmit src/types/category.ts src/types/priority.ts</automated>
  </verify>
  <done>Category and Priority interfaces match API schema, CRUD payload types exported, both files compile without errors</done>
</task>

</tasks>

<verification>
- All three type files exist in src/types/
- TypeScript compilation passes for all files
- Interfaces match TalTech API field names exactly (taskName not name, categoryName not name)
- All types are properly exported
- Nullable fields use `| null` consistently
- Date fields are strings (not Date objects)
</verification>

<success_criteria>
- [ ] src/types/task.ts exists with Task, CreateTaskPayload, UpdateTaskPayload
- [ ] src/types/category.ts exists with Category, CreateCategoryPayload, UpdateCategoryPayload
- [ ] src/types/priority.ts exists with Priority, CreatePriorityPayload, UpdatePriorityPayload
- [ ] All interfaces match API schema field names exactly
- [ ] TypeScript compilation passes without errors
- [ ] Types follow established patterns from auth.ts
</success_criteria>

<output>
After completion, create `.planning/phases/03-todo-core/03-01-SUMMARY.md`
</output>
