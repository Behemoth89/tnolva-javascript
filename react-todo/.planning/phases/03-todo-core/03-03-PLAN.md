---
phase: 03-todo-core
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/features/dashboard/components/TaskCard.tsx
  - src/features/dashboard/components/TaskList.tsx
  - src/features/dashboard/components/TaskModal.tsx
  - src/features/dashboard/pages/DashboardPage.tsx
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - TASK-05

must_haves:
  truths:
    - "User can see their tasks displayed as cards"
    - "User can create a new task via modal dialog"
    - "User can edit an existing task via modal dialog"
    - "User can delete a task with confirmation"
    - "User can toggle task completion with checkbox"
  artifacts:
    - path: "src/features/dashboard/components/TaskCard.tsx"
      provides: "Individual task card with name, category, priority, due date, checkbox"
      min_lines: 40
    - path: "src/features/dashboard/components/TaskList.tsx"
      provides: "Task list container mapping tasks to cards"
      min_lines: 20
    - path: "src/features/dashboard/components/TaskModal.tsx"
      provides: "Modal form for creating/editing tasks"
      min_lines: 80
    - path: "src/features/dashboard/pages/DashboardPage.tsx"
      provides: "Updated dashboard integrating task components"
      exports: ["DashboardPage"]
  key_links:
    - from: "src/features/dashboard/pages/DashboardPage.tsx"
      to: "src/stores/useTaskStore.ts"
      via: "Zustand store hook"
      pattern: "useTaskStore\\(\\)"
    - from: "src/features/dashboard/components/TaskModal.tsx"
      to: "src/stores/useTaskStore.ts"
      via: "createTask/updateTask actions"
      pattern: "(createTask|updateTask)\\("
---

<objective>
Build the task management UI components: task cards, task list, task creation/edit modal, and wire them into the DashboardPage. Implements full task CRUD with the zinc/amber design language.

Purpose: Users can view, create, edit, delete, and complete tasks through an intuitive card-based interface.
Output: Task UI components integrated into the existing dashboard layout.
</objective>

<execution_context>
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-todo-core/03-CONTEXT.md
@src/features/dashboard/pages/DashboardPage.tsx
@src/stores/useTaskStore.ts
@src/stores/useCategoryStore.ts
@src/stores/usePriorityStore.ts
@src/types/task.ts
@src/types/category.ts
@src/types/priority.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/task.ts:
```typescript
export interface Task {
  id: string;
  taskName: string;
  taskSort: number;
  createdDt: string;
  dueDt: string | null;
  isCompleted: boolean;
  isArchived: boolean;
  todoCategoryId: string | null;
  todoPriorityId: string | null;
  syncDt: string | null;
}
```

From src/stores/useTaskStore.ts:
```typescript
interface TaskStore {
  tasks: Task[];
  isLoading: boolean;
  error: string | null;
  fetchTasks: () => Promise<void>;
  createTask: (payload: CreateTaskPayload) => Promise<void>;
  updateTask: (payload: UpdateTaskPayload) => Promise<void>;
  deleteTask: (id: string) => Promise<void>;
  toggleTaskCompletion: (id: string) => Promise<void>;
  clearError: () => void;
}
```

From src/stores/useCategoryStore.ts:
```typescript
interface CategoryStore {
  categories: Category[];
  isLoading: boolean;
  error: string | null;
  fetchCategories: () => Promise<void>;
  // ... CRUD actions
}
```

From src/stores/usePriorityStore.ts:
```typescript
interface PriorityStore {
  priorities: Priority[];
  isLoading: boolean;
  error: string | null;
  fetchPriorities: () => Promise<void>;
  // ... CRUD actions
}
```

Design decisions from CONTEXT.md:
- D-01: Card-based layout (not table or simple list)
- D-02: Card shows: task name, category, priority icon, due date, completion checkbox
- D-03: Cards follow zinc/amber design language
- D-04: Modal dialog for create/edit (not inline form or separate page)
- D-05: Form fields: task name, category dropdown, priority dropdown, due date picker
- D-13: Loading states use amber-500 ring spinner (same as ProtectedRoute)
- D-14: Error states use red-500 banner (consistent with auth pages)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskCard and TaskList components</name>
  <files>src/features/dashboard/components/TaskCard.tsx, src/features/dashboard/components/TaskList.tsx</files>
  <action>Create two components for displaying tasks in a card-based layout.

src/features/dashboard/components/TaskCard.tsx:
- Props: `task: Task`, `onToggle: (id: string) => void`, `onEdit: (task: Task) => void`, `onDelete: (id: string) => void`
- Card styling: `bg-zinc-900 border border-zinc-800 rounded-lg p-4 hover:border-zinc-700 transition-colors`
- Layout: Flex row with checkbox on left, content in middle, actions on right
- Checkbox: amber-500 border when unchecked, amber-500 bg when checked, toggles completion
- Content area: Task name (text-zinc-100, font-medium, line-through when completed), category badge (zinc-800 bg, zinc-400 text, rounded px-2 py-0.5), due date (zinc-400 text, small)
- Actions: Edit button (pencil icon), Delete button (trash icon) - amber-500 hover
- Completed tasks: Apply opacity-60 and line-through to task name
- Import Task type from src/types/task.ts

src/features/dashboard/components/TaskList.tsx:
- Props: `tasks: Task[]`, `onToggle: (id: string) => void`, `onEdit: (task: Task) => void`, `onDelete: (id: string) => void`
- Layout: Grid with `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- Maps over tasks array, renders TaskCard for each
- If tasks array is empty, show placeholder text (Phase 4 handles proper empty states)
- Import TaskCard from same directory

Dark theme colors from CONTEXT.md:
- Page background: zinc-950
- Card background: zinc-900
- Borders: zinc-800
- Primary text: zinc-100
- Muted text: zinc-400
- Accent: amber-500
</action>
  <verify>
    <automated>npx tsc --noEmit src/features/dashboard/components/TaskCard.tsx src/features/dashboard/components/TaskList.tsx</automated>
  </verify>
  <done>TaskCard renders task data with checkbox/edit/delete, TaskList maps tasks to cards in responsive grid, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskModal for create/edit operations</name>
  <files>src/features/dashboard/components/TaskModal.tsx</files>
  <action>Create a modal dialog component for creating and editing tasks.

src/features/dashboard/components/TaskModal.tsx:
- Props: `isOpen: boolean`, `onClose: () => void`, `onSubmit: (payload: CreateTaskPayload | UpdateTaskPayload) => void`, `task?: Task` (undefined for create mode), `categories: Category[]`, `priorities: Priority[]`, `isSubmitting: boolean`
- Modal overlay: `fixed inset-0 bg-black/50 flex items-center justify-center z-50`
- Modal content: `bg-zinc-900 border border-zinc-800 rounded-xl p-6 w-full max-w-md`
- Form fields:
  - Task name: text input, required, placeholder "Enter task name"
  - Category: select dropdown with categories, first option "No category" with null value
  - Priority: select dropdown with priorities, first option "No priority" with null value
  - Due date: date input (type="date"), optional
- Form validation: inline error for empty task name, touched state tracking
- Submit button: amber-500 bg, disabled when submitting or name empty
- Cancel button: zinc-800 bg, zinc-400 text
- Form state: useState for name, categoryId, priorityId, dueDate, errors, touched
- On submit: construct payload object, call onSubmit, reset form
- On close: reset form state
- Title: "Create Task" or "Edit Task" based on whether task prop exists
- Pre-fill form with task data when editing (task prop provided)
- Import types from src/types/task.ts, src/types/category.ts, src/types/priority.ts

Form validation rules:
- Task name required (min 1 character, max 100 characters)
- Clear error on input change
- Error styling: red-500 text, small font, below input
</action>
  <verify>
    <automated>npx tsc --noEmit src/features/dashboard/components/TaskModal.tsx</automated>
  </verify>
  <done>Modal opens/closes with isOpen prop, form validates task name, submits correct payload, pre-fills in edit mode, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 3: Wire task components into DashboardPage</name>
  <files>src/features/dashboard/pages/DashboardPage.tsx</files>
  <action>Update DashboardPage to integrate task components with loading and error states.

Implementation:
- Import useTaskStore, useCategoryStore, usePriorityStore
- Import TaskList, TaskModal from components
- Import ProtectedRoute spinner pattern for loading state
- Import error banner pattern from auth pages

DashboardPage structure:
1. useEffect to fetch tasks, categories, priorities on mount
2. Show spinner (amber-500 ring) while tasks loading
3. Show error banner (bg-red-500/10 border border-red-500/20 text-red-500) if task error
4. Main content area:
   - Header with "Tasks" title and "Add Task" button (amber-500)
   - TaskList component with tasks from store
   - TaskModal controlled by isOpen state
5. Handlers:
   - handleAddTask: opens modal in create mode
   - handleEditTask: opens modal in edit mode with task data
   - handleDeleteTask: calls store deleteTask, confirms with window.confirm
   - handleToggleTask: calls store toggleTaskCompletion
   - handleSubmitTask: calls createTask or updateTask based on mode, closes modal

State management:
- useState for modal open/close
- useState for editing task (null for create mode)
- All data from Zustand stores (no local state for tasks)

Design from CONTEXT.md:
- D-06: Modal accessible from button in main content area
- D-13: Loading spinner matches ProtectedRoute (amber-500 ring)
- D-14: Error banner matches auth pages (red-500/10 bg)
</action>
  <verify>
    <automated>npx tsc --noEmit src/features/dashboard/pages/DashboardPage.tsx</automated>
  </verify>
  <done>DashboardPage shows tasks from store, Add Task button opens modal, CRUD operations work through store, loading/error states display correctly</done>
</task>

</tasks>

<verification>
- DashboardPage renders task list with cards
- Add Task button opens modal with form
- Form submits to store createTask action
- Edit button opens modal pre-filled with task data
- Delete button removes task after confirmation
- Checkbox toggles task completion
- Loading spinner shows during initial fetch
- Error banner shows on API failure
- TypeScript compilation passes for all files
</verification>

<success_criteria>
- [ ] TaskCard displays task name, category, priority, due date, checkbox
- [ ] TaskList renders tasks in responsive grid layout
- [ ] TaskModal handles both create and edit modes
- [ ] DashboardPage integrates all components with store
- [ ] Loading state shows amber-500 spinner during fetch
- [ ] Error state shows red-500 banner on failure
- [ ] All CRUD operations work through Zustand stores
- [ ] TypeScript compilation passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-todo-core/03-03-SUMMARY.md`
</output>
