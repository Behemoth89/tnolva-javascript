---
phase: 04-settings-integration
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/features/dashboard/components/Sidebar.tsx
autonomous: true
requirements: [TASK-06, SET-01, SET-02]
user_setup: []

must_haves:
  truths:
    - "Sidebar renders with category checkboxes, date range buttons, and show completed toggle"
    - "Sidebar shows 'All Tasks' at top and 'Uncategorized' at bottom"
    - "Category task counts displayed next to each category name"
    - "Selected categories highlighted with amber-500 background"
  artifacts:
    - path: "src/features/dashboard/components/Sidebar.tsx"
      provides: "Sidebar component with filtering controls"
      exports: ["Sidebar"]
    - path: "src/stores/useTaskStore.ts"
      provides: "Filter state (from Plan 01)"
      contains: "toggleCategoryId"
  key_links:
    - from: "src/features/dashboard/components/Sidebar.tsx"
      to: "src/stores/useTaskStore.ts"
      via: "useTaskStore selectors for filter state and actions"
      pattern: "useTaskStore.*selectedCategoryIds"
    - from: "src/features/dashboard/components/Sidebar.tsx"
      to: "src/stores/useCategoryStore.ts"
      via: "useCategoryStore for categories list"
      pattern: "useCategoryStore.*categories"
---

<objective>
Create Sidebar component with category filtering, due date filter, and completed task toggle

Purpose: Replace empty aside placeholder with functional filtering sidebar that controls task display
Output: New Sidebar component wired to useTaskStore filter state
</objective>

<execution_context>
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings-integration/04-CONTEXT.md
@.planning/phases/04-settings-integration/04-RESEARCH.md
@.planning/phases/04-settings-integration/04-VALIDATION.md
@.planning/phases/04-settings-integration/04-01-SUMMARY.md
@.planning/phases/04-settings-integration/04-02-SUMMARY.md

<interfaces>
From src/stores/useTaskStore.ts (extended by Plan 01):
```typescript
// Filter state fields:
selectedCategoryIds: string[];
selectedDateRange: string | null;
showCompleted: boolean;

// Filter actions:
toggleCategoryId: (id: string) => void;
clearCategoryFilter: () => void;
setSelectedDateRange: (range: string | null) => void;
toggleShowCompleted: () => void;
```

From src/stores/useCategoryStore.ts (existing):
```typescript
interface CategoryStore {
  categories: Category[];
  isLoading: boolean;
  fetchCategories: () => Promise<void>;
}
```

From src/types/category.ts:
```typescript
interface Category {
  id: string;
  categoryName: string;
  categorySort: number;
}
```

From src/components/EmptyState.tsx (created by Plan 01):
```typescript
interface EmptyStateProps {
  icon: React.ElementType;
  title: string;
  subtitle?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sidebar component with category filtering section</name>
  <files>src/features/dashboard/components/Sidebar.tsx</files>
  <read_first>
    - src/features/dashboard/pages/DashboardPage.tsx — sidebar placeholder to replace
    - src/stores/useTaskStore.ts — filter state fields and actions (from Plan 01)
    - src/stores/useCategoryStore.ts — categories list
    - src/components/EmptyState.tsx — component to use for empty category state
  </read_first>
  <action>
    Create src/features/dashboard/components/Sidebar.tsx with three sections:

    SECTION 1: Category Filtering (top section)
    1. Import useTaskStore and useCategoryStore
    2. Read from useTaskStore: selectedCategoryIds, toggleCategoryId, clearCategoryFilter
    3. Read from useCategoryStore: categories
    4. Render "All Tasks" button at top:
       - Always visible, resets filter when clicked
       - Calls clearCategoryFilter() on click
       - Highlighted (amber-500 bg) when selectedCategoryIds is empty
       - Shows total task count: useTaskStore((s) => s.tasks).length
    5. Render category checkboxes below "All Tasks":
       - Sort categories by categorySort ascending
       - Each category: checkbox + label + task count
       - Checkbox checked if category.id in selectedCategoryIds
       - onChange calls toggleCategoryId(category.id)
       - Row highlighted with bg-amber-500/10 when selected (per D-04)
       - Task count: tasks.filter(t => t.todoCategoryId === category.id).length
    6. Render "Uncategorized" at bottom (per D-02):
       - Shows tasks where todoCategoryId is null, undefined, or empty string
       - Task count: tasks.filter(t => !t.todoCategoryId).length
       - Checkbox checked if "uncategorized" is in selectedCategoryIds (use special string "__uncategorized__")
       - Highlighted when selected

    SECTION 2: Due Date Filter (middle section, per D-16)
    1. Read from useTaskStore: selectedDateRange, setSelectedDateRange
    2. Section header: "Due Date" text, text-sm font-medium text-zinc-300
    3. Render date range buttons (per D-17):
       - "Today" — value: "today"
       - "This Week" — value: "this-week"
       - "Next Week" — value: "next-week"
       - "Overdue" — value: "overdue"
       - "No Date" — value: "no-date"
    4. Each button: small pill-style button, bg-zinc-800 text-zinc-400, rounded-full px-3 py-1 text-xs
    5. Selected button: bg-amber-500 text-zinc-900
    6. Clicking selected button deselects it (sets to null)
    7. Flex wrap layout for buttons: flex flex-wrap gap-2

    SECTION 3: Show Completed Toggle (bottom section, per D-19)
    1. Read from useTaskStore: showCompleted, toggleShowCompleted
    2. Checkbox + label: "Show completed"
    3. Checkbox checked when showCompleted is true
    4. onChange calls toggleShowCompleted()
    5. Label text-zinc-400, checkbox uses amber-500 accent

    STYLING:
    - Container: w-64 bg-zinc-900 min-h-[calc(100vh-64px)] border-r border-zinc-800 p-4
    - Sections separated by border-b border-zinc-800 pb-4 mb-4
    - Section headers: text-sm font-medium text-zinc-300 mb-3
    - Use dark theme throughout (zinc-900 bg, zinc-800 borders, zinc-400 text, amber-500 accent)

    Per D-01: Multi-select category filtering with checkboxes
    Per D-02: "All Tasks" at top, "Uncategorized" at bottom
    Per D-03: Task counts next to each category
    Per D-04: Selected categories highlighted with amber-500
    Per D-05: Category data fetched on DashboardPage mount (not in Sidebar)
    Per D-16: Due date filter below categories section
    Per D-17: Single-select date range buttons
    Per D-19: "Show completed" checkbox in sidebar
    Per D-20: Completed tasks hidden by default
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -30 || echo "No test framework ready — verify manually: test -f src/features/dashboard/components/Sidebar.tsx && grep 'toggleCategoryId' src/features/dashboard/components/Sidebar.tsx && grep 'setSelectedDateRange' src/features/dashboard/components/Sidebar.tsx && grep 'toggleShowCompleted' src/features/dashboard/components/Sidebar.tsx"</automated>
  </verify>
  <done>Sidebar component exists with three sections: category checkboxes (All Tasks + categories + Uncategorized), due date range buttons (today/this-week/next-week/overdue/no-date), and show completed toggle. All wired to useTaskStore filter state.</done>
  <acceptance_criteria>
    - File src/features/dashboard/components/Sidebar.tsx exists
    - Exports function Sidebar
    - Imports useTaskStore and useCategoryStore
    - Contains "All Tasks" button that calls clearCategoryFilter
    - Contains category checkboxes that call toggleCategoryId
    - Contains "Uncategorized" option for tasks without category
    - Contains date range buttons: "Today", "This Week", "Next Week", "Overdue", "No Date"
    - Date range buttons call setSelectedDateRange
    - Contains "Show completed" checkbox that calls toggleShowCompleted
    - Uses bg-amber-500/10 or similar for selected category highlighting
    - Shows task counts next to categories
    - Uses dark theme colors (zinc-900, zinc-800, zinc-400, amber-500)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Sidebar component is a single file, single task
- Depends on Plan 01 (store extensions) and Plan 02 (EmptyState for potential use)
- DashboardPage integration happens in Plan 04 (Wave 3)
</verification>

<success_criteria>
- Sidebar.tsx created with all three filtering sections
- Category section: All Tasks button, category checkboxes with counts, Uncategorized option
- Due date section: 5 date range buttons with single-select behavior
- Completed section: Show completed checkbox toggle
- All controls wired to useTaskStore filter state and actions
- Dark theme styling consistent with project conventions
- Amber-500 highlighting for selected categories
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-integration/04-03-SUMMARY.md`
</output>
