---
phase: 04-settings-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/useTaskStore.ts
  - src/components/EmptyState.tsx
autonomous: true
requirements: [TASK-06, SET-01, SET-02, SET-03]
user_setup: []

must_haves:
  truths:
    - "useTaskStore exposes selectedCategoryIds, selectedDateRange, showCompleted state"
    - "Filter state persists across page reload via Zustand persist"
    - "EmptyState component renders with icon, title, and optional subtitle"
  artifacts:
    - path: "src/stores/useTaskStore.ts"
      provides: "Filter state with persist middleware"
      contains: "selectedCategoryIds"
    - path: "src/components/EmptyState.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState"]
  key_links:
    - from: "src/stores/useTaskStore.ts"
      to: "localStorage"
      via: "persist middleware partialize"
      pattern: "persist.*partialize"
---

<objective>
Extend useTaskStore with filter state and create reusable EmptyState component

Purpose: Foundation for sidebar filtering — store holds filter preferences, EmptyState provides consistent empty messaging across the app
Output: Extended store with persist, new EmptyState component
</objective>

<execution_context>
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Kool/tnolva-javascript/react-todo/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings-integration/04-CONTEXT.md
@.planning/phases/04-settings-integration/04-RESEARCH.md
@.planning/phases/04-settings-integration/04-VALIDATION.md

<interfaces>
From src/stores/useTaskStore.ts (current state — will be extended):
```typescript
interface TaskStore {
  tasks: Task[];
  isLoading: boolean;
  error: string | null;
  fetchTasks: () => Promise<void>;
  createTask: (payload: CreateTaskPayload) => Promise<void>;
  updateTask: (payload: UpdateTaskPayload) => Promise<void>;
  deleteTask: (id: string) => Promise<void>;
  toggleTaskCompletion: (id: string) => Promise<void>;
  clearError: () => void;
}
```

From src/components/EmptyState.tsx (new — to be created):
```typescript
interface EmptyStateProps {
  icon: React.ElementType;
  title: string;
  subtitle?: string;
}
export function EmptyState({ icon, title, subtitle }: EmptyStateProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend useTaskStore with filter state and persist</name>
  <files>src/stores/useTaskStore.ts</files>
  <read_first>
    - src/stores/useTaskStore.ts — current store structure to extend
    - src/stores/useAuthStore.ts — existing persist pattern to mirror
  </read_first>
  <action>
    Extend the TaskStore interface and implementation with three filter state fields:

    1. Add to interface:
       - `selectedCategoryIds: string[]` — array of selected category IDs for multi-select filtering (per D-01)
       - `selectedDateRange: string | null` — selected due date range: "today" | "this-week" | "next-week" | "overdue" | "no-date" | null (per D-17)
       - `showCompleted: boolean` — whether to show completed tasks, defaults to false (per D-20)

    2. Add action creators to interface and implementation:
       - `setSelectedCategoryIds: (ids: string[]) => void` — replaces the entire selectedCategoryIds array
       - `toggleCategoryId: (id: string) => void` — adds id if not present, removes if present (multi-select toggle per D-01)
       - `clearCategoryFilter: () => void` — sets selectedCategoryIds to empty array (resets to "All Tasks")
       - `setSelectedDateRange: (range: string | null) => void` — sets the date range filter
       - `toggleShowCompleted: () => void` — toggles showCompleted boolean

    3. Wrap store with Zustand persist middleware:
       - Import: `import { persist } from 'zustand/middleware'`
       - Use `partialize` to ONLY persist filter fields: `selectedCategoryIds`, `selectedDateRange`, `showCompleted`
       - Do NOT persist tasks array, isLoading, or error — these should be fresh on reload
       - localStorage key: `'task-filter-state'`

    4. Default values:
       - selectedCategoryIds: [] (empty = show all tasks)
       - selectedDateRange: null (no date filter active)
       - showCompleted: false (completed tasks hidden by default)

    Per D-12, D-13, D-14, D-15: All filter state lives in useTaskStore with persist.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -30 || echo "No test framework ready — verify manually: grep 'selectedCategoryIds' src/stores/useTaskStore.ts && grep 'persist' src/stores/useTaskStore.ts && grep 'partialize' src/stores/useTaskStore.ts"</automated>
  </verify>
  <done>useTaskStore has selectedCategoryIds (string[]), selectedDateRange (string|null), showCompleted (boolean) with persist middleware using partialize, and action creators for toggleCategoryId, setSelectedCategoryIds, clearCategoryFilter, setSelectedDateRange, toggleShowCompleted</done>
  <acceptance_criteria>
    - src/stores/useTaskStore.ts contains `selectedCategoryIds: string[]`
    - src/stores/useTaskStore.ts contains `selectedDateRange: string | null`
    - src/stores/useTaskStore.ts contains `showCompleted: boolean`
    - src/stores/useTaskStore.ts contains `import { persist }` from 'zustand/middleware'
    - src/stores/useTaskStore.ts contains `partialize` in persist config
    - src/stores/useTaskStore.ts contains `toggleCategoryId` function
    - src/stores/useTaskStore.ts contains `clearCategoryFilter` function
    - src/stores/useTaskStore.ts contains `setSelectedDateRange` function
    - src/stores/useTaskStore.ts contains `toggleShowCompleted` function
    - persist config name is 'task-filter-state'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create reusable EmptyState component</name>
  <files>src/components/EmptyState.tsx</files>
  <read_first>
    - src/features/dashboard/components/TaskList.tsx — current inline empty state to replace
    - src/features/dashboard/pages/DashboardPage.tsx — loading spinner pattern for visual consistency
  </read_first>
  <action>
    Create a new shared component at src/components/EmptyState.tsx with the following:

    1. Props interface:
       - `icon: React.ElementType` — Heroicon component to render (e.g., InboxIcon, FolderIcon)
       - `title: string` — main heading text
       - `subtitle?: string` — optional descriptive text below title

    2. Component structure (per D-10: icon + title + subtitle pattern):
       - Centered container with flex column layout
       - Icon rendered at 48x48 (h-12 w-12), text-zinc-500 color, centered
       - Title text: text-lg font-medium text-zinc-300, mt-4
       - Subtitle text (if provided): text-sm text-zinc-500, mt-1
       - Full container: flex flex-col items-center justify-center py-12 px-4

    3. Usage variants to support (do NOT create variant props — caller passes icon/text):
       - No tasks: icon=InboxIcon, title="No tasks yet", subtitle="Click 'Add Task' to create one"
       - No categories: icon=FolderIcon, title="No categories", subtitle="Add categories in Settings"
       - No priorities: icon=FlagIcon, title="No priorities", subtitle="Add priorities in Settings"
       - Filtered empty: icon=FunnelIcon, title="No tasks in this category", subtitle="Try selecting a different category"

    4. Import icons from @heroicons/react/24/outline — the library is already installed (v2.2.0)

    Use dark theme colors consistent with existing patterns: zinc-950 background, zinc-300 for title, zinc-500 for subtitle/icon.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -30 || echo "No test framework ready — verify manually: test -f src/components/EmptyState.tsx && grep 'EmptyState' src/components/EmptyState.tsx && grep 'icon' src/components/EmptyState.tsx && grep 'title' src/components/EmptyState.tsx"</automated>
  </verify>
  <done>EmptyState component exists at src/components/EmptyState.tsx with icon, title, subtitle props, centered layout, dark theme styling, and exports EmptyState function</done>
  <acceptance_criteria>
    - File src/components/EmptyState.tsx exists
    - Contains `export function EmptyState`
    - Accepts `icon` prop of type React.ElementType
    - Accepts `title` prop of type string
    - Accepts optional `subtitle` prop of type string
    - Renders icon at h-12 w-12 size
    - Uses text-zinc-300 for title, text-zinc-500 for subtitle
    - Uses flex flex-col items-center justify-center py-12 layout
    - Imports from @heroicons/react/24/outline
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Both tasks are independent (Wave 1) — no file conflicts
- useTaskStore.ts and EmptyState.tsx are separate files
- Store extensions verified by checking interface fields and persist config
- EmptyState verified by checking file existence, exports, and prop types
</verification>

<success_criteria>
- useTaskStore has all three filter state fields with correct types
- persist middleware configured with partialize containing only filter fields
- All five action creators exist: setSelectedCategoryIds, toggleCategoryId, clearCategoryFilter, setSelectedDateRange, toggleShowCompleted
- EmptyState component exported from src/components/EmptyState.tsx
- EmptyState accepts icon (ElementType), title (string), subtitle (string?) props
- EmptyState uses dark theme colors consistent with project conventions
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-integration/04-01-SUMMARY.md`
</output>
