services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: https://tnolva-express-api.proxy.itcollege.ee/api/v1
    ports:
      - "88:80"
    restart: unless-stopped
    container_name: react-todo-express
